vector<int> b;
        ListNode *begin=head;
        ListNode *tmp=begin->next;
        int count=0;
        while(tmp!=NULL){
            count++;
            b.push_back(begin->val);
            begin=tmp;
            tmp=tmp->next;
        }
        vector<int> a(count);
        return a;
